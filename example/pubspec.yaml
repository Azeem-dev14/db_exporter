name: db_exporter_example
description: Exports the three most-used Flutter SQL databases with db_exporter.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  db_exporter:
    path: ../

  # The three most-used SQL packages, one dataset each.
  sqflite: ^2.3.3                 # Schools
  sqlite3: ^2.4.0                 # Bookstore
  drift: ^2.20.0                  # Music
  sqlite3_flutter_libs: ^0.5.24   # native SQLite for drift and sqlite3

  path: ^1.9.0
  path_provider: ^2.1.4

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
